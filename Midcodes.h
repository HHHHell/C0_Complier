#pragma once

#include "string"
#include "vector"

using namespace std;
class Midcodes
{
	vector<string[5]> clist;
public:
	Midcodes();
	void insert(string* slist, int n);
	void output(string filename);
};